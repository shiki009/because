{
  "name": "because",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "extension:zip": "cd extension && zip -r ../extension.zip . -x '*.DS_Store'"
  },
  "dependencies": {
    "@ai-sdk/groq": "^3.0.22",
    "@vercel/analytics": "^2.0.1",
    "ai": "^6.0.77"
  }
}
